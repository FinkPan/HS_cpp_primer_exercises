下列代码中，为什么第一个指针的初始化是合法的，而第
二个则不合法？ 
          int i = 42; 
          void *p = &i; 
          long *lp = &i; 

答:
因为void* 可以指向任意类型的指针.而long*指针只能指向long类型指针,
而不能指向int类型指针.

void* ，它可以保存任何类型对象的地址：
void* 表明该指针与一地址值相关，但不清楚存储在此地址上的对象的类型。