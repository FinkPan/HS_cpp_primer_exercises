解释一下在上几个习题的程序实现中你用了哪种迭代器，并说明原因。

答:
用了vector 的 iterator 
因为标准库为每一种标准容器（包括 vector）定义了一种迭代器类型。迭代器 
类型提供了比下标操作更通用化的方法：所有的标准库容器都定义了相应的迭代 
器类型，而只有少数的容器支持下标操作。因为迭代器对所有的容器都适用，现 
代 C++ 程序更倾向于使用迭代器而不是下标操作访问容器元素，即使对支持下 
标操作的 vector 类型也是这样。

这题目好难回答其原因~~~~ 我用了vector容器,自然用vector对应的iterator~~~