给定下列定义： 
     char cval;  int ival;   unsigned int ui; 
     float fval;             double dval; 
 
指出可能发生的（如果有的话）隐式类型转换： 
     (a) cval = 'a' + 3;        (b) fval = ui - ival 
* 1.0; 
     (c) dval = ui * fval;      (d) cval = ival + fval 
+ dval; 
 